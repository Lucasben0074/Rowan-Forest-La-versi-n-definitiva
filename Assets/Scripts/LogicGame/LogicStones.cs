using UnityEngine;

public class LogicStones : MonoBehaviour
{
    private int stoneCount = 0;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if(stoneCount == 0)
        {
            //el npc tiene el id 2.
        }
    }
}
